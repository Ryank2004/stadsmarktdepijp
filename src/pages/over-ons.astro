---
import Layout from "@layout/layout.astro";
import { Image } from "astro:assets";
import slager from "@images/lachende-slager.webp";
import kaas from "@images/kaas-snijden.webp";
import brood from "@images/brood-aangeven.webp"; 

// SEO
const seoTitle = "Over Ons";
const seoDescription = "Leer meer over Stadsmarkt De Pijp. Een team van vakmensen die met passie werken aan kwaliteitsproducten. Van slager tot visspecialist, groente-expert, bakker en kaasspecialist.";
---

<Layout title={seoTitle} description={seoDescription}>
    <section class="team-section">
        <h2 class="section-title">
            Over <span class="title-spacing">o</span>ns
        </h2>

        <div class="content-wrapper">
            
            <!-- Sectie 1: Tekst links, Foto rechts -->
            <div class="team-row first-row">
                <div class="text-content">
                    <h4 class="first-title">Met de handen</h4>
                    <p class="subtitle">Wat we doen</p>
                    <p>
                        Bij Stadsmarkt De Pijp draait het om mensen die hun vak verstaan.
                        Snijden, selecteren, adviseren, elke dag opnieuw, met aandacht en ervaring.
                        Ons team van vakmensen kent hun producten door en door.
                    </p>
                    <p>
                        Van slager tot visspecialist, van groente-expert tot bakker en kaasspecialist.
                        Elk teamlid brengt vakmanschap mee en is trots op wat ze doen.
                        Samen zorgen we ervoor dat je altijd de beste kwaliteit krijgt.
                    </p>
                </div>
                <div class="image-content">
                    <div class="image-parallax-container">
                        <Image 
                            src={slager}
                            alt="Vakmanschap"
                            width={800}
                            height={1000}
                            class="section-image"
                        />
                    </div>
                </div>
            </div>

            <!-- Sectie 2: Foto links, Tekst rechts -->
            <div class="team-row reverse">
                <div class="text-content">
                    <h4>In het moment</h4>
                    <p class="subtitle">Hoe we het doen</p>
                    <p>
                        Een praatje bij de toonbank, advies over wat er die avond op tafel komt,
                        een glimlach als je binnenstapt. Het zijn de kleine momenten die het
                        verschil maken bij ons.
                    </p>
                    <p>
                        We nemen de tijd voor je, want goed eten begint met persoonlijke aandacht.
                        Onze vakmensen kennen hun klanten, herinneren voorkeuren,
                        en denken actief mee over wat bij je wensen past.
                    </p>
                </div>
                <div class="image-content">
                    <div class="image-parallax-container">
                        <Image 
                            src={kaas}
                            alt="Verbinding"
                            width={800}
                            height={1000}
                            class="section-image"
                        />
                    </div>
                </div>
            </div>

            <!-- Sectie 3: Tekst links, Foto rechts -->
            <div class="team-row">
                <div class="text-content">
                    <h4>Voor elkaar</h4>
                    <p class="subtitle">Waarom we het doen</p>
                    <p>
                        Samen zorgen we ervoor dat boodschappen doen meer is dan een 
                        lijstje afvinken. Het is kiezen voor kwaliteit, voor contact, 
                        voor bewust leven.
                    </p>
                    <p>
                        We geloven dat iedereen recht heeft op toegang tot eerlijk, lekker 
                        en gezond eten. Dat is waar ons team elke dag voor staat.
                    </p>
                </div>
                <div class="image-content">
                    <div class="image-parallax-container">
                        <Image 
                            src={brood}
                            alt="Het team"
                            width={800}
                            height={1000}
                            class="section-image"
                        />
                    </div>
                </div>
            </div>

        </div>

        <!-- Afsluitende sectie -->
        <div class="closing-section">
            <p class="closing-text">
                Kom langs en ontmoet ons team. We staan voor je klaar.
            </p>
        </div>
    </section>
</Layout>

<style>
    .team-section {
        padding: 10.5rem var(--container-padding-md) 8rem;
        background-color: var(--color-light);
        color: var(--color-dark);
    }

    .section-title {
        font-size: 11vw;
        margin-bottom: 1rem;
        text-align: left;
        line-height: 0.9;
    }

    .title-spacing {
        letter-spacing: -2px;
    }

    .content-wrapper {
        max-width: 1400px;
        margin: 0 auto;
    }

    /* Team rows */
    .team-row {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        margin-bottom: 4rem;
        opacity: 0;
    }

    .first-row {
        opacity: 1;
    }

    /* Text content */
    .text-content {
        flex: 1;
    }

    .text-content h4 {
        font-size: clamp(36px, 7vw, 64px);
        color: var(--color-primary);
        margin-bottom: 0.5rem;
        line-height: 0.9;
    }

    .first-title {
        display: none;
    }

    .subtitle {
        font-family: var(--font-heading);
        font-size: clamp(18px, 3vw, 22px);
        color: var(--color-dark);
        text-transform: uppercase;
        margin-bottom: 1.5rem;
        font-weight: 600;
    }

    .text-content p {
        font-size: clamp(18px, 1.5vw, 22px);
        margin-bottom: 1.2rem;
        color: var(--text-color);
        /* font-weight: 500; */
    }

    /* Image content */
    .image-content {
        flex: 1;
    }

    /* Image container */
    .image-parallax-container {
        width: 100%;
        height: 350px;
        overflow: hidden;
        border-radius: var(--border-radius-md);
        position: relative;
    }

    .section-image {
        width: 100%;
        height: 100%;
        object-fit: cover;
        display: block;
    }

    /* Tablet */
    @media screen and (min-width: 687px) {
        .team-section {
            padding: 10.5rem var(--container-padding-lg) 6rem;
        }

        .section-title {
            margin-bottom: 4rem;
        }

        .team-row {
            gap: 3rem;
            margin-bottom: 6rem;
        }

        .image-parallax-container {
            height: 450px;
        }
    }

    /* Desktop */
    @media screen and (min-width: 900px) {
        .team-section {
            padding: 12rem var(--container-padding-lg) 8rem;
        }

        .section-title {
            font-size: 9.7vw;
            margin-bottom: 8rem;
            text-align: center;
        }

        .first-title {
            display: block;
        }

        .title-spacing {
            letter-spacing: -14px;
        }

        .team-row {
            flex-direction: row;
            align-items: center;
            gap: 6rem;
            margin-bottom: 10rem;
        }

        /* Reverse row - Foto links, Tekst rechts */
        .team-row.reverse {
            flex-direction: row-reverse;
        }

        .text-content {
            max-width: 600px;
        }

        .image-content {
            max-width: 550px;
        }

        .image-parallax-container {
            height: 500px;
        }
    }

    /* Large desktop */
    @media screen and (min-width: 1200px) {
        .team-row {
            gap: 8rem;
            margin-bottom: 12rem;
        }
    }

    @media screen and (min-width: 1240px) {
        .text-content h4 {
            white-space: nowrap;
        }
    }

    /* Closing section */
    .closing-section {
        max-width: 1400px;
        margin: 0 auto;
        text-align: center;
        padding: 2rem var(--container-padding-md) 0;
    }

    .closing-text {
        font-size: clamp(20px, 3vw, 44px);
        line-height: 1.4;
        color: var(--color-primary);
        font-family: var(--font-heading);
        text-transform: uppercase;
        font-weight: 600;
    }

    @media screen and (min-width: 687px) {
        .closing-section {
            padding: 2rem var(--container-padding-lg) 4rem;
        }
    }

    @media screen and (min-width: 900px) {
        .closing-section {
            padding: 2rem var(--container-padding-lg) 4rem;
        }
    }
</style>

<script src="/src/scripts/fade-in.js"></script>